# name: Deploy nextjs App

# on:
#   # push:
#   #   branches: [main]

# jobs:
#   quality-and-build:
#     name: 1. Quality & Build
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'

#       - name: Install & Check
#         run: |
#           npm ci
#           npm run lint
#           npm run type-check

#       - name: Build Next.js
#         run: npm run build
#         env:
#           DATABASE_URL: ${{ secrets.DATABASE_URL }}
#           AUTH_SECRET: ${{ secrets.AUTH_SECRET }}

#       - name: Archiver le build
#         run: tar -czf deploy.tar.gz .next public package.json package-lock.json next.config.* prisma

#       - name: Transfert vers VPS
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.VPS_HOST }}
#           username: ${{ secrets.VPS_USER }}
#           key: ${{ secrets.VPS_SSH_KEY }}
#           source: "deploy.tar.gz"
#           target: "/opt/kazier"

#   finalize-deploy:
#     name: 2. Execution VPS
#     needs: quality-and-build
#     runs-on: ubuntu-latest
#     steps:
#       - name: ⚡ Sync DB & Restart PM2
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.VPS_HOST }}
#           username: ${{ secrets.VPS_USER }}
#           key: ${{ secrets.VPS_SSH_KEY }}
#           script: |
#             cd /opt/kazier
#             tar -xzf deploy.tar.gz
#             rm deploy.tar.gz

#             # 1. Installation propre sans lancer les scripts de dev (Husky)
#             export NODE_ENV=production
#             npm install --omit=dev --ignore-scripts

#             # 2. Prisma (On garde migrate deploy si vous avez des migrations)
#             npx prisma migrate deploy || npx prisma db push

#             # 3. Redémarrage PM2 avec le chemin complet si nécessaire
#             # On utilise 'npx pm2' pour être sûr de le trouver
#             npx pm2 reload kazier-app --update-env || npx pm2 start npm --name "kazier-app" -- start -- -p 3000

#             npx pm2 save
