- checklite avant commit 📋 Checklist avant chaque commit

  # 1. Formater le code

  npm run format

  # 2. Vérifier la qualité

  npm run quality

  # 3. (Optionnel) Tester le build Docker

  docker build -t kazier:test .
